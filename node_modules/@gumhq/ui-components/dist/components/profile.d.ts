interface ProfileMetadata {
    name: string;
    bio: string;
    username: string;
    following: number;
    followers: number;
    avatar: string;
    connect?: {
        following: boolean;
        follow: () => Promise<any | void>;
        unfollow: () => Promise<any | void>;
    };
}
declare function Profile({ data }: {
    data: ProfileMetadata;
}): JSX.Element;
export { Profile, ProfileMetadata };
