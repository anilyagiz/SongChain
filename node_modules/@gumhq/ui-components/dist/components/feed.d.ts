import { ProfileMetadata } from "./profile";
import { PostMetadata } from "./post";
interface FeedMetadata {
    posts: {
        post: PostMetadata;
        profile: ProfileMetadata;
    }[];
    skip: number;
    show: number;
    gap: number;
}
declare function Feed({ posts, skip, show, gap }: FeedMetadata): JSX.Element;
export { Feed, FeedMetadata };
