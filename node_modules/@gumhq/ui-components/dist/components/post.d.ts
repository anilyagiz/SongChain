import { ProfileMetadata } from "./profile";
interface PostMetadata {
    type: string;
    content: {
        content: string;
        format: string;
    };
    reply?: () => any;
}
declare function Post({ data, profileData }: {
    data: PostMetadata;
    profileData: ProfileMetadata;
}): JSX.Element;
export { Post, PostMetadata };
